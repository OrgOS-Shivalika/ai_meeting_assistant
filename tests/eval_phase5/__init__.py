"""Phase 5F — hybrid graph RAG eval harness.

Entry point: `run_eval.py` (also importable for the 5F ship test).

Cases live in `cases.py`. Each case targets the canonical fixture from
`tests/fixtures/canonical_org.py`, so cases AND fixtures evolve
together — no eval drift from data drift.
"""
